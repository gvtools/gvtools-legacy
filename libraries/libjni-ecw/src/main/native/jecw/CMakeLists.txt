set(LIB_NAME jecw)

FILE(GLOB SOURCES "*.c*")

set (JAVA_INCLUDE_PATH $ENV{JDK_HEADER_DIR})
set (JAVA_INCLUDE_PATH2 $ENV{JDK_HEADER_DIR}/linux)

include_directories(
	${CMAKE_SOURCE_DIR}/include
	${JAVA_INCLUDE_PATH}
	${JAVA_INCLUDE_PATH2}
	${ECW_INCLUDE_DIR}
)

SET(LIBRARIES_OPTIMIZED 
	${ECW_LIBRARIES}
)

SET(LIBRARIES_DEBUG
	${ECW_LIBRARIES}
)

SETUP_JNILIB(${LIB_NAME})


