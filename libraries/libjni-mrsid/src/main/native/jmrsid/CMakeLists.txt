set(LIB_NAME jmrsid)

SET(HEADER_PATH ${CMAKE_SOURCE_DIR}/include)
FILE(GLOB HEADERS "${HEADER_PATH}/*.h*")
FILE(GLOB SOURCES "*.c*")

set (JAVA_INCLUDE_PATH "$ENV{JDK_HEADER_DIR}")
set (JAVA_INCLUDE_PATH2 "$ENV{JDK_HEADER_DIR_SYS}")

include_directories(
	${CMAKE_SOURCE_DIR}/include
	${JAVA_INCLUDE_PATH}
	${JAVA_INCLUDE_PATH2}
	${MRSID_INCLUDE_DIR}
	${MRSID_INCLUDE_DIR2}
	${MRSID_INCLUDE_DIR3}
	${MRSID_INCLUDE_DIR4}
	${MRSID_INCLUDE_DIR5}
	${MRSID_INCLUDE_DIR6}
	${MRSID_INCLUDE_DIR7}
	${MRSID_INCLUDE_DIR8}
)
IF(UNIX)
SET(LIBRARIES_OPTIMIZED 
    ${MRSID_LIBRARY}
)
SET(LIBRARIES_DEBUG
    ${MRSID_LIBRARY}
)
ENDIF(UNIX)

IF(WIN32)
	SET(LIBRARIES_OPTIMIZED
		${MRSID_LIBRARY_W1}
		${MRSID_LIBRARY_W2}
	)
	SET(LIBRARIES_DEBUG
		${MRSID_LIBRARY_W1}
		${MRSID_LIBRARY_W2}
	)
ENDIF(WIN32)

SETUP_JNILIB(${LIB_NAME})


